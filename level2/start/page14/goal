축하합니다! 레벨 2를 완료했습니다!

pwd 를 입력해서 지금 어디에 있는지 확인해 보세요.

이번에 배운 명령어:
  cd ..  — 한 단계 위 폴더로 돌아가기
  pwd    — 현재 위치 확인하기

다음 레벨로 가려면:
(첫 줄은 "이 저장소의 최상위 폴더로 이동" 이라는 뜻입니다. 그대로 복사해서 붙여넣으세요.)

  cd "$(git rev-parse --show-toplevel)"
  cd level3/start
  cat note
