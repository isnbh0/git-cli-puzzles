축하합니다! 레벨 3을 완료했습니다!

이번에 배운 명령어:
  mkdir 폴더       — 새 폴더 만들기
  cp 원본 사본     — 파일 복사
  mv 원본 새이름   — 파일 이동 또는 이름 변경
  rm 파일          — 파일 삭제 (되돌릴 수 없음)

이것으로 기본 터미널 명령어를 모두 배웠습니다.

다음 레벨로 가려면:
  cd ~
  git checkout world-level4
  cd start
  cat note
